# from pytest_bdd import scenario, given, when, then
# from application.calendar_config import get_service
# import pytest
# import datetime
# import os
#
#
# # Scenario 1
# @scenario('../../features/calendar_api/calendar_api_사용.feature', 'credential key가 올바르지 않는 경우 권한이 없어서 실패')
# def invalid_credential_key():
#     pass
#
#
# @given("잘못된 credentail key를 가지고 있다")
# def step_impl():
#     raise NotImplementedError(u'STEP: Given 잘못된 credentail key를 가지고 있다')
#
#
# @when("Google calendar api를 사용하려고 할 때")
# def step_impl():
#     raise NotImplementedError(u'STEP: When Google calendar api를 사용하려고 할 때')
#
#
# @then("credential key가 잘못 되었으므로 실패한다")
# def step_impl():
#     raise NotImplementedError(u'STEP: Then credential key가 잘못 되었으므로 실패한다')
#
#
# @given("올바른 scope와 cedentail key값을 가지고 있다")
# def step_impl():
#     raise NotImplementedError(u'STEP: Given 올바른 scope와 cedentail key값을 가지고 있다')
# #
# #
# # @when("잘못된 service를 build하려고 할 때")
# # def step_impl():
# #     raise NotImplementedError(u'STEP: When 잘못된 service를 build하려고 할 때')
# #
# #
# # @then("service가 잘못됐으므로 실패")
# # def step_impl():
# #     raise NotImplementedError(u'STEP: Then service가 잘못됐으므로 실패')
